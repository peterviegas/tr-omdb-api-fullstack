import React from 'react';
import RenderMoviesList from '../Movie/RenderMoviesList';

const WatchList: React.FC = () => {
    return (
        <div className='watchlist'>
            {/* <RenderMoviesList movies={} searchedMovieName={''} onClick={} /> */}
        </div>
    );
};

export default WatchList;
